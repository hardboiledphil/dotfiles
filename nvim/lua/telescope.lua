-- Lots of defaults already set so don't need to paste in the 
-- example config unless you want to override something
require('telescope').load_extension('fzf')
require('telescope').load_extension('coc')

